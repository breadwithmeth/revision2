{
  "name": "inventory-revision-backend",
  "version": "1.0.0",
  "description": "REST-бэкенд для пересчёта товаров",
  "main": "dist/index.js",
  "scripts": {
    "dev": "tsx src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "prisma:migrate": "prisma migrate dev && prisma generate",
    "prisma:push": "prisma db push && prisma generate"
  },
  "dependencies": {
    "fastify": "^4.24.3",
    "@prisma/client": "^5.6.0"
  },
  "devDependencies": {
    "prisma": "^5.6.0",
    "typescript": "^5.2.2",
    "tsx": "^4.4.0",
    "@types/node": "^20.8.10"
  },
  "engines": {
    "node": ">=18"
  }
}
